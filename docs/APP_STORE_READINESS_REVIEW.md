Rà soát khả năng submit ListingForge — 13/09/2026

**Kết luận: chưa nên submit bản hiện tại.** Đây là đánh giá code và build trong workspace, gồm cả các thay đổi chưa commit của Claude. P1 là việc cần xử lý trước khi gửi review; P2 là lỗi cần sửa/kiểm thử trước phát hành. Các bước tái hiện dưới đây được suy ra từ code, trừ những kiểm tra build/test được ghi rõ đã chạy. Không thay đổi code ứng dụng trong lần rà soát này.

Đã kiểm tra:

- XcodeGen và unit test trên iPhone 17 Simulator, iOS 26.5: XCTest báo **116 test passed, 1 skipped, 0 failed**; khi tính các bộ tham số có 120 lượt passed. Test Vision segmentation bị bỏ qua theo kiểm tra hỗ trợ của môi trường.
- Build **Release cho iPhone thật**, `CODE_SIGNING_ALLOWED=NO`: thành công. Đây chưa phải archive được ký hay kết quả Validate App trên App Store Connect.
- Đọc Info.plist của binary Release: SDK `iphoneos26.5`, Xcode `2660`, deployment target `17.0`; API vẫn là `http://localhost:3000`.
- Launch app đã cài trên simulator thành công. Chưa chạy toàn bộ hành trình người dùng với production backend, chưa gọi AI tính phí, gửi OTP hay xóa tài khoản thật trong lần rà soát này.
- App có icon, entitlement Sign in with Apple, permission strings cho camera/photos và privacy manifest trong bundle. Các mục này không chứng minh cấu hình distribution hay nội dung khai báo đã đầy đủ.

Các điểm cần xử lý, theo mức độ ưu tiên:

1. **[P1] Binary Release mặc định gọi localhost.** `ios/project.yml:30`, `ios/ListingForge/Support/AppConfig.swift:15`. Đã xác nhận trực tiếp trong binary, không chỉ suy đoán từ cấu hình. Trên iPhone reviewer, localhost là chính iPhone đó; đăng nhập và API sẽ không tới máy dev. Tách cấu hình Debug/Release, dùng production HTTPS và chặn build Release nếu endpoint thiếu hoặc là loopback. Kiểm tra lại endpoint trong archive cuối cùng và đăng nhập trên mạng ngoài máy dev. Liên quan [Guideline 2.1](https://developer.apple.com/app-store/review/guidelines/#app-completeness).

2. **[P1] Generate xong không có màn hình xem hoặc lấy kết quả.** `ios/ListingForge/Features/Capture/CaptureView.swift:89` nhận `onGenerated` bằng closure rỗng. `ProductsView.swift:25` chỉ hiển thị tên/category; không có điều hướng tới asset hay export. Server có thể trả thành công và trừ credits, nhưng app đóng sheet mà không đưa listing cho người dùng. Cần nối Review, hiển thị lỗi từng marketplace, cho copy/share và mở lại kết quả từ Products. Kiểm tra: tạo một listing, sử dụng nội dung, đóng/mở app rồi truy cập lại listing đó. Liên quan [Guideline 2.1](https://developer.apple.com/app-store/review/guidelines/#app-completeness).

3. **[P1] Cache dùng chung giữa các tài khoản.** `ios/ListingForge/Features/Products/ProductsView.swift:12` query toàn bộ CachedProduct. Model không có owner; `ListingForgeApp.swift:20` dùng một container toàn app. `AuthStore.swift:85` và `:94` chỉ xóa session khi sign out/delete; AppEnvironment cũng giữ ProductStore/GenerationStore qua lần đổi tài khoản. Tài khoản B có thể thấy cache hoặc kết quả của A trên cùng thiết bị. Cần cô lập cache/store theo account, bỏ phản hồi đang chạy của account cũ và dọn dữ liệu khi xóa account. Kiểm tra A tạo sản phẩm → sign out → B đăng nhập, cả khi offline, và trường hợp chuyển account trong lúc request đang chạy. Liên quan [Guideline 1.6](https://developer.apple.com/app-store/review/guidelines/#data-security).

4. **[P1] Thiếu Privacy Policy và thông tin hỗ trợ trong app.** `ios/ListingForge/Features/Settings/SettingsView.swift:19` chỉ có Account/Delete; AuthView cũng không có link chính sách. Chưa tìm thấy link support ở các view khác. Cần URL thật, truy cập được từ app, nội dung khớp dịch vụ đang chạy; không dùng URL placeholder. App Store Connect chưa được kiểm tra. Tham chiếu [Guidelines 5.1.1(i), 1.5](https://developer.apple.com/app-store/review/guidelines/#privacy).

5. **[P1] Luồng AI chưa giải thích nơi gửi dữ liệu hoặc xin phép rõ ràng.** `ios/ListingForge/Features/Capture/MarketplacesView.swift:184` gọi generate trực tiếp. Backend `api/src/lib/ai/anthropic.ts:67` gửi ảnh sang Anthropic; dữ liệu văn bản cũng được gửi cho provider. Không tìm thấy bước consent trong Swift. Ảnh/đặc điểm do người dùng nhập có thể chứa thông tin cá nhân; nút cấp quyền camera không giải thích việc chia sẻ này. Cần nêu dữ liệu và bên nhận trước khi gửi, có lựa chọn từ chối, và không gọi AI khi chưa đồng ý. Đối chiếu [Guideline 5.1.2(i), bao gồm third-party AI](https://developer.apple.com/app-store/review/guidelines/#data-use-and-sharing).

6. **[P1] Privacy inventory vẫn là bản chỉ có đăng nhập.** `ios/ListingForge/PrivacyInfo.xcprivacy:22` chỉ khai báo email/user ID, còn Photos or Videos nằm trong TODO. Trong khi `ProductDetailsView.swift:184` đã upload cutout và gửi nội dung sản phẩm. Cần cập nhật inventory cho ảnh và văn bản người dùng, rà soát tên nhận từ Apple, dữ liệu gửi cho provider/tracing, rồi đồng bộ manifest, policy và App Privacy trong Connect. Manifest không tự thay thế câu trả lời App Privacy. Xem [Apple App Privacy Details](https://developer.apple.com/app-store/app-privacy-details/).

7. **[P1] Delete Account chưa hoàn tất vòng đời dữ liệu/quyền đăng nhập.** Swift gửi `authorizationCode`, nhưng `api/src/app/api/auth/apple/route.ts:29` không xử lý nó để phục vụ token revocation. `api/src/app/api/account/delete/route.ts:20` bỏ qua lỗi tìm org/xóa products; route không xóa storage object, không xóa tên người dùng có thể còn trong tên workspace, không revoke Apple token. `api/src/lib/session.ts:38` chỉ xác minh JWT: session 90 ngày đã cấp vẫn qua xác thực sau tombstone vì không kiểm tra account đã bị xóa. Ở Swift, SettingsView không hiển thị `auth.errorMessage` nên lỗi delete chỉ khiến nút hoạt động trở lại. Cần hoàn thiện backend cleanup/revocation, báo lỗi và cho thử lại trong app, cùng việc dọn cache ở mục 3. Kiểm tra cả lỗi cleanup và token cũ sau delete. Tham chiếu [Apple: Offering account deletion](https://developer.apple.com/support/offering-account-deletion-in-your-app/).

8. **[P1] Tài khoản mới chưa có đường sử dụng credits hoàn chỉnh.** Không thấy cấp credits lúc tạo account trong `api/src/lib/users.ts`; ledger rỗng trả balance 0 (`credits.ts:70`), còn generate kiểm tra số dư (`generate.ts:73`). Với tài khoản chưa được cấp credits từ bên ngoài, luồng chính dừng ở 402. `MarketplacesView.swift:110` còn TODO paywall; không có StoreKit purchase/restore trong app. Cần chọn bản phát hành miễn phí hoạt động được hoặc hoàn tất billing trước khi quảng bá Pro. Nếu bán digital credits/subscription theo mô hình SPEC, triển khai và kiểm thử StoreKit, entitlement server, restore subscription và refund. Đây là luồng chưa hoàn thiện, chưa có bằng chứng app đang dùng checkout sai quy định. Tham chiếu [Guidelines 2.1, 3.1](https://developer.apple.com/app-store/review/guidelines/#payments); ngoại lệ thanh toán phụ thuộc storefront/mô hình kinh doanh.

9. **[P2] Lỗi generate có thể bị xử lý như thành công của lần trước.** `GenerationStore.swift:60` không xóa `result` trước request hoặc khi thất bại; `MarketplacesView.swift:193` kiểm tra property dùng chung sau await. Tái hiện: generate A thành công, generate B nhận 402/500 → result vẫn của A, UI gọi callback/dismiss thay vì giữ lỗi B. Cần trả kết quả riêng cho từng request, reset trạng thái và chặn request trùng. Đồng thời quote tại `GenerationStore.swift:45` chưa loại bỏ phản hồi cũ: đổi selection nhanh có thể hiển thị giá của selection trước. Cần test success → failure và quote trả về đảo thứ tự; khóa hoặc đồng bộ selection với request đang tính phí.

10. **[P2] Luồng capture/details mất dữ liệu hoặc tạo trùng khi quay lại.** `ProductDetailsView.swift:184` chỉ gửi ảnh đầu; `CaptureView.swift:86` xóa toàn bộ các góc sau create. `.sheet(item: $createdProduct)` ở ProductDetailsView dùng cùng state cho record đã lưu và trình bày màn hình: Back từ Marketplaces làm state nil, Continue lại tạo product mới. Task save vẫn có thể hoàn tất sau khi người dùng đã đóng sheet. Cần lưu identity của draft/product độc lập với navigation, xử lý kết quả request theo đúng draft và giữ đủ ảnh đến khi upload được xác nhận. Không thể xử lý multi-angle đầy đủ chỉ bằng Swift khi hợp đồng API vẫn nhận một ảnh.

11. **[P2] Các chi tiết cần sửa/kiểm tra thêm trước phát hành.** `ProductDetailsView.swift:205` cắt đặc điểm theo dấu phẩy, làm sai nội dung có phẩy hợp lệ. `GenerationStore.swift:89` trả pass khi marketplace không có asset dù nằm trong failures. `CaptureView.swift:87` gọi `camera.series.reset()` thay vì `camera.resetSeries()`, không nhả AE/AWB ở phần cứng. Build Release báo ba cảnh báo capture AVFoundation object không Sendable tại `CameraSession.swift:103,117,202`; chưa có bằng chứng crash, nhưng cần rà soát queue/isolation và test camera trên máy thật, không chỉ tắt cảnh báo.

Các kiểm tra còn thiếu trước khi quyết định submit:

- Production backend online; Apple sign-in lần đầu/lần sau/private relay; OTP sai/hết hạn/gửi lại; reviewer có cách truy cập đầy đủ tính năng. Không dựa vào tài khoản dev đã được cấp credits thủ công để kết luận onboarding hoạt động.
- TestFlight trên iPhone thật và thiết bị nhỏ: camera bị từ chối, PhotosPicker, Vision cutout, foreground/background, mạng chậm/mất mạng, cancel/back/retry, hết credits, generation thành công một phần; Dynamic Type, VoiceOver và dark mode.
- Archive có ký và Validate App; provisioning có Sign in with Apple; privacy report của archive; icon/screenshot thực tế, age rating, Support/Privacy URLs và review notes trong App Store Connect. Hiện chưa có quyền truy cập/kiểm tra các cấu hình này.
- SDK hiện dùng đã vượt mức tối thiểu iOS 26/Xcode 26 áp dụng từ 28/04/2026; kiểm tra lại yêu cầu ở ngày upload. Deployment target iOS 17 không cần đổi chỉ vì yêu cầu build SDK. [Apple SDK minimum requirements](https://developer.apple.com/news/upcoming-requirements/?id=04282026a).

Các test có tên “App Store compliance” hiện chỉ bảo vệ một số cấu hình. Ví dụ `declaresCollectedData()` chưa yêu cầu photos dù comment manifest nói có; test delete chỉ kiểm tra POST tới endpoint; không kiểm chứng storage cleanup/revoke. Chú thích cho rằng external payment link luôn bị từ chối cũng không phản ánh đầy đủ ngoại lệ storefront hiện tại. Vì vậy 116 test passed không phải chứng nhận qua App Review.

Ưu tiên thực hiện: endpoint Release và hoàn tất hành trình Generate/Review; song song xử lý account isolation/deletion và privacy/AI consent; sau đó hoàn thiện mô hình credits/billing, kiểm thử máy thật và archive/Connect. Quyết định phê duyệt cuối cùng thuộc Apple.
